// Return values are always enclosed in parentheses - recommended approach
// Or the return value should start from the same line, instead of moving it to next line
export default function SecondComponent() {
    return <div className='SecondComponent'>Second Component</div>
}